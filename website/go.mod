// Hugo module for the documentation site. Deliberately a separate module from
// the CLI: theme dependencies must not reach the released binary's go.mod.
module github.com/Flagsmith/flagsmith-cli/website

go 1.26

require github.com/imfing/hextra v0.12.3 // indirect
